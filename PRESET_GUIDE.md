# 预设管理系统使用指南

## 概述

预设管理系统允许你保存和管理常用的 RPC URL、合约地址和 ABI，实现快速切换和组合使用。

## 三维独立设计

预设系统采用**三维独立**设计，每个维度都可以单独管理：

### 1. RPC URL 预设（网络维度）
保存常用的 RPC 端点，例如：
- Ethereum 主网
- BSC 主网  
- Polygon 主网
- 测试网络
- 自定义 RPC

### 2. 合约地址预设（合约维度）
保存常用的合约地址，例如：
- USDC
- USDT
- DAI
- 你自己部署的合约
- 常用的 DeFi 协议合约

### 3. ABI 预设（接口维度）
保存常用的 ABI 接口定义，例如：
- ERC20 标准接口
- ERC721 NFT 接口
- Uniswap V2 Pair
- 自定义合约 ABI

## 使用方法

### 保存预设

#### 保存 RPC 预设
1. 在"RPC URL"输入框中输入 RPC 地址
2. 点击旁边的"💾 保存"按钮
3. 在弹出的对话框中输入预设名称（如"我的节点"）
4. 确认保存

#### 保存合约地址预设
1. 在"合约地址"输入框中输入合约地址
2. 点击旁边的"💾 保存"按钮
3. 输入预设名称（如"USDC 主网"）
4. 确认保存

#### 保存 ABI 预设
1. 在 ABI 输入框中输入或粘贴 ABI
2. 点击"💾 保存"按钮
3. 输入预设名称（如"ERC20 完整接口"）
4. 确认保存

### 使用预设

#### 从下拉列表选择
1. 点击对应的预设下拉框
2. 从列表中选择你要使用的预设
3. 相应的内容会自动填充到输入框中

#### 自由组合
你可以任意组合三个维度的预设，例如：
- **组合 1**: Ethereum 主网 + USDC + ERC20 ABI
- **组合 2**: BSC 主网 + 自定义合约 + 自定义 ABI
- **组合 3**: 测试网 + 测试合约 + ERC20 ABI

### 管理预设

#### 查看所有预设
点击"⚙️ 管理"按钮打开预设管理窗口，可以看到：
- 预设名称
- 创建时间
- 内容预览
- 操作按钮

#### 编辑预设
1. 在管理窗口中找到要编辑的预设
2. 点击"编辑"按钮
3. 修改预设名称
4. 确认保存

#### 删除预设
1. 在管理窗口中找到要删除的预设
2. 点击"删除"按钮
3. 再次点击"确认删除？"按钮
4. 预设被永久删除

> ⚠️ 删除操作不可恢复，请谨慎操作

## 自动保存与恢复

### 自动保存
系统会自动保存你最后使用的配置（延迟 1 秒）：
- 输入 RPC URL 后自动保存
- 输入合约地址后自动保存
- 输入 ABI 后自动保存

### 自动恢复
下次打开页面时，系统会自动恢复：
- 上次使用的 RPC URL
- 上次使用的合约地址
- 上次使用的 ABI

这样你可以继续上次的工作，无需重新输入。

## 内置预设

首次使用时，系统会自动创建一些常用预设：

### RPC 预设
- **Ethereum 主网**: `https://eth.llamarpc.com`
- **BSC 主网**: `https://bsc-dataseed.binance.org`
- **Polygon 主网**: `https://polygon-rpc.com`

### ABI 预设
- **ERC20 标准接口**: 包含 name、symbol、decimals、totalSupply、balanceOf

你可以：
- 直接使用这些预设
- 编辑它们的名称
- 删除不需要的预设
- 添加更多自定义预设

## 数据存储

所有预设数据都存储在浏览器的 localStorage 中：
- 数据只保存在本地，不会上传到服务器
- 清除浏览器数据会删除所有预设
- 不同浏览器的预设是独立的

## 使用场景

### 场景 1: 多链开发
你在多个链上开发，需要频繁切换网络：
1. 保存所有链的 RPC 为预设
2. 保存各链上的测试合约地址
3. 快速切换网络进行测试

### 场景 2: 多合约调用
你需要调用多个不同的合约：
1. 保存所有常用合约地址
2. 保存对应的 ABI
3. 从下拉列表快速切换

### 场景 3: 团队协作
团队成员使用相同的测试环境：
1. 创建团队共用的预设
2. 导出预设（手动复制）
3. 其他成员导入（手动粘贴）

### 场景 4: 学习测试
学习区块链开发，测试各种合约：
1. 保存教程中的合约地址
2. 保存对应的 ABI
3. 随时回来练习和测试

## 技巧与最佳实践

### 命名建议
使用清晰、有意义的名称：
- ✅ "Ethereum 主网 - Alchemy"
- ✅ "USDC (Ethereum)"
- ✅ "ERC20 完整接口"
- ❌ "RPC 1"
- ❌ "合约A"

### 定期整理
定期清理不再使用的预设，保持列表整洁。

### 备份重要预设
对于重要的配置，可以：
1. 复制预设内容到文本文件
2. 保存到云笔记或 Git 仓库
3. 需要时重新导入

### 分类管理
使用前缀进行分类：
- `[主网] USDC`
- `[测试] 测试合约`
- `[项目A] 自定义合约`

## 常见问题

### Q: 预设会丢失吗？
A: 预设存储在浏览器的 localStorage 中，只要不清除浏览器数据就不会丢失。

### Q: 能导出预设到其他浏览器吗？
A: 目前需要手动复制内容。后续版本可能会添加导入/导出功能。

### Q: 预设有数量限制吗？
A: localStorage 通常有 5-10MB 的限制，可以存储大量预设。

### Q: 如何清除所有预设？
A: 清除浏览器数据，或在浏览器控制台执行：
```javascript
localStorage.clear()
```

### Q: 预设会同步到其他设备吗？
A: 不会。预设只存储在当前浏览器中。

## 反馈与建议

如果你有任何建议或发现问题，欢迎提出 Issue！

